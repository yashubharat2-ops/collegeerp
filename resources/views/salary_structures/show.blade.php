@extends('layouts.app')
@section('title', $structure->name)
@section('content')<div class="panel"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="panel-title">{{ $structure->name }} <span class="text-sm font-normal text-slate-500">({{ $structure->code }})</span></h2><p class="panel-subtitle">{{ $structure->description ?: 'Salary component definition' }}</p></div><div class="flex gap-2">@can('create', App\Models\SalaryComponent::class)<a class="button" href="{{ route('salary-components.create', ['salary_structure_id' => $structure->id]) }}">+ Add component</a>@endcan<a class="button !bg-slate-200 !text-slate-700" href="{{ route('salary-structures.index') }}">Back</a></div></div><div class="mt-8 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="py-3">Component</th><th>Type</th><th>Calculation</th><th>Basis</th><th>Value</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($structure->components as $component)<tr class="border-b"><td class="py-3 font-medium">{{ $component->name }} <span class="text-xs text-slate-500">{{ $component->code }}</span></td><td>{{ ucfirst($component->component_type) }}</td><td>{{ ucfirst($component->calculation_type) }}</td><td>{{ $component->basis ?: '—' }}</td><td>{{ number_format((float)$component->value, 2) }}{{ $component->calculation_type === 'percentage' ? '%' : '' }}</td><td>{{ ucfirst($component->status) }}</td><td class="text-right"><div class="flex justify-end gap-2">@can('update', $component)<a class="text-xs text-indigo-600" href="{{ route('salary-components.edit', $component) }}">Edit</a>@endcan @can('delete', $component)<form method="POST" action="{{ route('salary-components.destroy', $component) }}">@csrf @method('DELETE')<button class="text-xs text-rose-600" type="submit">Delete</button></form>@endcan</div></td></tr>@empty<tr><td colspan="7" class="py-6 text-slate-500">No components defined.</td></tr>@endforelse</tbody></table></div></div>@endsection
