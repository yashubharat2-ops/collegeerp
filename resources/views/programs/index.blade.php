@extends('layouts.app') @section('title','Programs') @section('content')<div class="panel"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="panel-title">Programs</h2><p class="panel-subtitle">Manage academic programs within the active college.</p></div>@can('create', App\Models\Program::class)<a class="button" href="{{ route('programs.create') }}">+ New program</a>@endcan</div>
<form method="GET" action="{{ route('programs.index') }}" class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><input class="input" type="search" name="search" value="{{ $search }}" placeholder="Search name or code"><select class="input" name="status"><option value="">All statuses</option><option value="active" @selected($status === 'active')>Active</option><option value="inactive" @selected($status === 'inactive')>Inactive</option></select><div class="flex gap-2"><button class="button" type="submit">Filter</button>@if($search !== '' || $status)<a class="button !bg-slate-200 !text-slate-700" href="{{ route('programs.index') }}">Clear</a>@endif</div></form>
<div class="mt-8 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="py-3">Name</th><th>Code</th><th>Short name</th><th>Department</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($programs as $program)<tr class="border-b"><td class="py-3 font-medium">{{ $program->name }}@if($program->description)<p class="mt-0.5 max-w-xs truncate text-xs text-slate-500" title="{{ $program->description }}">{{ $program->description }}</p>@endif</td><td>{{ $program->code }}</td><td>{{ $program->short_name ?? '—' }}</td><td>{{ $program->department?->name ?? '— (college level)' }}</td><td><span class="rounded-full px-2 py-0.5 text-xs font-semibold {{ $program->status === 'active' ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-200 text-slate-600' }}">{{ ucfirst($program->status) }}</span></td><td class="text-right"><div class="flex flex-wrap items-center justify-end gap-2">@can('update', $program)<a class="text-xs font-semibold text-indigo-600 hover:underline" href="{{ route('programs.edit', $program) }}">Edit</a>@endcan @can('delete', $program)<form method="POST" action="{{ route('programs.destroy', $program) }}" onsubmit="return confirm(@js('Delete program '.$program->name.'? This can be undone by an administrator.'))">@csrf @method('DELETE')<button class="text-xs font-semibold text-rose-600 hover:underline" type="submit">Delete</button></form>@endcan</div></td></tr>@empty<tr><td class="py-6 text-slate-500" colspan="6">No programs found.</td></tr>@endforelse</tbody></table></div>
<div class="mt-4 flex flex-wrap items-center justify-between gap-2"><p class="text-xs text-slate-500">Showing {{ $programs->firstItem() ?? 0 }}–{{ $programs->lastItem() ?? 0 }} of {{ $programs->total() }} programs.</p>{{ $programs->links() }}</div></div>@endsection
