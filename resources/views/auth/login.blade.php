@extends('layouts.guest')
@section('content')<div class="rounded-2xl bg-white p-8 text-slate-900 shadow-2xl"><div class="mb-8"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-600">College ERP</p><h1 class="mt-2 text-3xl font-bold">Welcome back</h1><p class="mt-2 text-sm text-slate-500">Sign in to your administration workspace.</p></div>@if(session('status'))<div class="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif @if($errors->any())<div class="mb-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('login.store') }}" class="space-y-5">@csrf<div><label class="label" for="email">Email</label><input class="input" id="email" name="email" type="email" value="{{ old('email') }}" required autofocus></div><div><label class="label" for="password">Password</label><input class="input" id="password" name="password" type="password" required></div><label class="flex items-center gap-2 text-sm text-slate-600"><input type="checkbox" name="remember" value="1"> Remember me</label><button class="button w-full" type="submit">Sign in</button></form><a class="mt-6 block text-center text-sm text-indigo-600" href="{{ route('password.request') }}">Forgot your password?</a></div>@endsection
