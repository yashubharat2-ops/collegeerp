@extends('layouts.guest')
@section('content')<div class="rounded-2xl bg-white p-8 text-slate-900 shadow-2xl"><h1 class="text-2xl font-bold">Reset your password</h1><p class="mt-2 text-sm text-slate-500">We will email a reset link if the account exists.</p>@if(session('status'))<div class="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}" class="mt-6 space-y-5">@csrf<label class="label" for="email">Email</label><input class="input" id="email" name="email" type="email" required><button class="button w-full" type="submit">Send reset link</button></form><a class="mt-6 block text-center text-sm text-indigo-600" href="{{ route('login') }}">Back to sign in</a></div>@endsection
