@extends('layouts.app') @section('title','Class / Section Academic Management') @section('content')<p class="mb-5 text-slate-500">Operational view of Platform sections. Sections are not managed here.</p><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($items as $s)<a class="card hover:border-indigo-400" href="{{route('academic-sections.show',$s)}}"><h2 class="font-semibold">{{$s->program?->name}} · {{$s->name}}</h2><p class="text-sm text-slate-500">{{$s->academicYear?->name}} · {{$s->campus?->name}}</p><p class="mt-3 text-sm">Subjects: {{$s->subject_count}} · Faculty assignments: {{$s->assignments_count}}</p></a>@empty<p>No sections available.</p>@endforelse</div>{{$items->links()}}@endsection
