@extends('layouts.app')
@section('title', 'Library Reports')
@section('content')
<div class="panel"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="panel-title">Library Reports</h2><p class="panel-subtitle">Read-only circulation and fine summaries for the active college.</p></div></div>
<div class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">@foreach($summary as $label => $value)<div class="rounded-lg border border-slate-200 bg-slate-50 p-4"><div class="text-xs font-semibold uppercase tracking-wide text-slate-500">{{ $label }}</div><div class="mt-1 text-2xl font-semibold text-slate-800">{{ $value }}</div></div>@endforeach</div>
<div class="mt-6 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="py-2">Issued</th><th>Book</th><th>Member</th><th>Status</th></tr></thead><tbody>@forelse($transactions as $tx)<tr class="border-b"><td class="py-2">{{ $tx->issued_on?->format('d M Y') }}</td><td>{{ $tx->bookCopy->book->title ?? '—' }}</td><td>{{ $tx->libraryMember->member_code ?? '—' }}</td><td><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-700">{{ ucfirst($tx->status) }}</span></td></tr>@empty<tr><td colspan="4" class="py-4 text-slate-500">No circulation records found for the selected criteria.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $transactions->links() }}</div>
<div class="mt-8"><h3 class="panel-title text-lg">Fine summary and outstanding fines</h3><div class="mt-4 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="py-2">Status</th><th>Total</th><th>Assessed</th><th>Outstanding</th></tr></thead><tbody>@forelse($fines as $fine)<tr class="border-b"><td class="py-2">{{ ucfirst($fine->status) }}</td><td>{{ $fine->total }}</td><td>{{ number_format((float) $fine->amount, 2) }}</td><td>{{ number_format((float) $fine->outstanding, 2) }}</td></tr>@empty<tr><td colspan="4" class="py-4 text-slate-500">No fine data found for this college.</td></tr>@endforelse</tbody></table></div></div>
</div>
@endsection