@extends('layouts.app') @section('content')<div class="space-y-6"><h1 class="text-xl font-bold">Library Reports</h1><div class="grid grid-cols-2 gap-3 md:grid-cols-5">@foreach($summary as $label=>$value)<div class="rounded border bg-white p-3"><div class="text-xs uppercase text-slate-500">{{ $label }}</div><div class="text-2xl font-semibold">{{ $value }}</div></div>@endforeach</div><section class="overflow-x-auto rounded border bg-white"><table class="w-full text-left"><tr><th class="p-3">Issued</th><th>Book</th><th>Member</th><th>Status</th></tr>@forelse($transactions as $tx)<tr class="border-t"><td class="p-3">{{ $tx->issued_on }}</td><td>{{ $tx->bookCopy->book->title ?? '-' }}</td><td>{{ $tx->libraryMember->member_code ?? '-' }}</td><td>{{ ucfirst($tx->status) }}</td></tr>@empty<tr><td colspan="4" class="p-3">No circulation records found.</td></tr>@endforelse</table></section>{{ $transactions->links() }}<h2 class="font-semibold">Fine summary and outstanding fines</h2><table class="w-full rounded border bg-white text-left"><tr><th class="p-3">Status</th><th>Total</th><th>Assessed</th><th>Outstanding</th></tr>@foreach($fines as $fine)<tr class="border-t"><td class="p-3">{{ ucfirst($fine->status) }}</td><td>{{ $fine->total }}</td><td>{{ number_format((float)$fine->amount,2) }}</td><td>{{ number_format((float)$fine->outstanding,2) }}</td></tr>@endforeach</table></div>@endsection