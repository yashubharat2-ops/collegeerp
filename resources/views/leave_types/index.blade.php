@extends('layouts.app')
@section('title', 'Leave Types')
@section('content')<div class="panel"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="panel-title">Leave types</h2><p class="panel-subtitle">College-owned leave categories used by staff leave requests.</p></div>@can('create', App\Models\LeaveType::class)<a class="button" href="{{ route('leave-types.create') }}">+ New leave type</a>@endcan</div><form method="GET" class="mt-6 flex flex-wrap gap-3"><input class="input" type="search" name="search" value="{{ $filters['search'] ?? '' }}" placeholder="Search name or code"><select class="input" name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '') === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="button" type="submit">Filter</button></form><div class="mt-8 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="py-3">Name</th><th>Code</th><th>Max days / year</th><th>Requests</th><th>Status</th><th class="text-right">Actions</th></tr></thead><tbody>@forelse($leaveTypes as $type)<tr class="border-b"><td class="py-3 font-medium">{{ $type->name }}</td><td>{{ $type->code }}</td><td>{{ $type->max_days_per_year ?? '—' }}</td><td>{{ $type->requests_count }}</td><td>{{ ucfirst($type->status) }}</td><td class="text-right"><div class="flex justify-end gap-2">@can('update', $type)<a class="text-xs text-indigo-600" href="{{ route('leave-types.edit', $type) }}">Edit</a>@endcan @can('delete', $type)<form method="POST" action="{{ route('leave-types.destroy', $type) }}">@csrf @method('DELETE')<button class="text-xs text-rose-600" type="submit">Delete</button></form>@endcan</div></td></tr>@empty<tr><td colspan="6" class="py-6 text-slate-500">No leave types found.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $leaveTypes->links() }}</div></div>@endsection
