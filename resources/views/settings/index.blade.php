@extends('layouts.app') @section('title','Institutional settings') @section('content')<div class="panel"><h2 class="panel-title">Institutional settings</h2><p class="panel-subtitle">Settings are isolated to the active college.</p><form method="POST" action="{{ route('settings.update') }}" class="mt-6 grid gap-4 md:grid-cols-4">@csrf<input class="input" name="key" placeholder="setting.key" required><input class="input md:col-span-2" name="value" placeholder="Value"><select class="input" name="type"><option>string</option><option>integer</option><option>boolean</option><option>json</option></select><button class="button md:col-span-4 md:w-fit" type="submit">Save setting</button></form><div class="mt-8 space-y-3">@forelse($settings as $setting)<div class="flex justify-between rounded-xl bg-slate-50 p-4 text-sm"><span class="font-semibold">{{ $setting->key }}</span><span>{{ $setting->value }}</span></div>@empty<p class="text-sm text-slate-500">No settings configured.</p>@endforelse</div></div>@endsection
