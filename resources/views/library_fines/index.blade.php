@extends('layouts.app') @section('content')<div class="p-6"><h1 class="text-xl font-bold">Fines / Penalties</h1><table class="w-full mt-4"><tr><th>Member</th><th>Period</th><th>Amount</th><th>Status</th></tr>@foreach($fines as $fine)<tr><td>{{ $fine->member->member_code ?? '-' }}</td><td>{{ $fine->period_start }} – {{ $fine->period_end }}</td><td>{{ number_format((float)$fine->assessed_amount,2) }}</td><td>{{ $fine->status }}</td></tr>@endforeach</table>{{ $fines->links() }}</div>@endsection