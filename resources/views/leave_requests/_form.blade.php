@csrf<div class="grid gap-4 sm:grid-cols-2"><label class="field sm:col-span-2"><span>Employee</span><select class="input" name="faculty_id" required>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected((int)old('faculty_id', $leaveRequest?->faculty_id) === $employee->id)>{{ $employee->full_name }} ({{ $employee->employee_code }})</option>@endforeach</select>@error('faculty_id')<small class="error">{{ $message }}</small>@enderror</label><label class="field"><span>Leave type</span><select class="input" name="leave_type_id" required>@foreach($leaveTypes as $type)<option value="{{ $type->id }}" @selected((int)old('leave_type_id', $leaveRequest?->leave_type_id) === $type->id)>{{ $type->name }} ({{ $type->code }})</option>@endforeach</select>@error('leave_type_id')<small class="error">{{ $message }}</small>@enderror</label><label class="field"><span>From date</span><input class="input" type="date" name="from_date" value="{{ old('from_date', $leaveRequest?->from_date?->format('Y-m-d')) }}" required>@error('from_date')<small class="error">{{ $message }}</small>@enderror</label><label class="field"><span>To date</span><input class="input" type="date" name="to_date" value="{{ old('to_date', $leaveRequest?->to_date?->format('Y-m-d')) }}" required>@error('to_date')<small class="error">{{ $message }}</small>@enderror</label><label class="field sm:col-span-2"><span>Reason</span><textarea class="input" name="reason" rows="4" required>{{ old('reason', $leaveRequest?->reason) }}</textarea>@error('reason')<small class="error">{{ $message }}</small>@enderror</label></div><div class="mt-6 flex gap-2"><button class="button" type="submit">{{ $submitLabel }}</button><a class="button !bg-slate-200 !text-slate-700" href="{{ route('leave-requests.index') }}">Cancel</a></div>
